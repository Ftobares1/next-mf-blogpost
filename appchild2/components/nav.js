import * as React from 'react';

const Nav = () => {
  return (
    <nav
      style={{
        background: 'blue',
        width: '100%',
        height: '100px',
        color: 'white',
        textAlign: 'center',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '24px',
      }}>
      Child Application 2
    </nav>
  );
};

export default Nav;
